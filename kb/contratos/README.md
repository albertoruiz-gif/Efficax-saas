# Biblioteca de Contratos Base

10 tipos por país/región (RFD RF-24, spec 07-legal-contratos.md). Flujo:
Claude redacta → abogado local valida y visa → entra al catálogo servible con
versión (país, región, norma base, abogado validador, fecha).

Estado: catálogo definido; redacción Perú pendiente (tarea #39).
Ningún contrato de esta carpeta se sirve a un tenant sin `validado: true`.
