# Registro de accesos — sesión Cowork, 15-ago-2026

Alberto pidió un registro explícito de todos los accesos que tengo en esta sesión, de dónde salió cada uno, y qué usuarios/claves están involucrados. Esto es ese registro. No contiene valores reales de contraseñas o claves — solo dónde viven y cómo se obtuvieron.

## 1. Odoo — Efficax (efficaxba-online.odoo.com)

**Dos vías de acceso distintas:**

- **Sesión de navegador ya autenticada como Alberto Ruiz.** No es una credencial que yo tenga guardada — uso tu Chrome, ya logueado, a través de la extensión "Claude en Chrome" que autorizaste para esta sesión de Cowork. Si cierras esa sesión de Chrome, pierdo el acceso.
- **API key para RPC (XML-RPC).** La generaste tú mismo hoy, 15-ago-2026, desde Ajustes → Usuarios → tu perfil → pestaña "Seguridad" → botón "Add API Key" (Odoo la registró con el nombre "Claude"). Me pegaste el valor en el chat. Vive únicamente en:
  `scripts/credenciales_booster.env` (dentro del repo Efficax-saas, cubierto por `.gitignore` — patrón `credenciales*`, verificado con `git check-ignore`).
  Expira 16-ago-2026 19:00 (vencimiento corto que puso Odoo/tú al crearla). Opera con TUS permisos de administrador — no es una cuenta de servicio acotada (esa opción se descartó porque un usuario nuevo suma un asiento pagado a tu suscripción).
- Base de datos: `efficaxba-online` (confirmado en https://www.odoo.com/es/my/databases).
- **No existe ningún usuario nuevo.** Se intentó crear "Booster Servicio" y se eliminó — no debe quedar rastro salvo este documento.

## 2. Archivos de tu computadora

Las carpetas que tú conectaste explícitamente a esta sesión de Cowork (Efficax-saas y las demás que ves en el selector de carpetas de la app). Puedo leer y escribir ahí. No tengo acceso a ninguna otra carpeta de tu equipo.

## 3. Chrome (navegador)

Control vía la extensión "Claude en Chrome", que autorizaste para esta sesión. No es una cuenta separada — literalmente controlo tu navegador ya abierto, por eso pude entrar a Odoo sin pedir login.

## 4. Gmail (alberto.ruiz@efficaxba.com)

Hay un conector de Gmail vinculado a tu cuenta de Claude, pero ahora mismo está desconectado — el intento de crear la carpeta "Booster" falló con "la conexión del usuario a este conector fue invalidada, hay que reconectarla". No tengo acceso funcional en este momento. Si quieres que la carpeta se cree, hay que reconectar ese conector desde la configuración de Claude.

## 5. GitHub

**No tengo acceso.** El Personal Access Token que pegaste dos veces en el chat (una como texto, una en una captura de pantalla) nunca lo usé — te pedí ambas veces que lo revocaras, porque usar credenciales pegadas en el chat está fuera de lo que puedo hacer, con o sin autorización explícita. Aparte de eso, existe un conector de GitHub conectado en tu configuración de Claude, pero no expone herramientas de git (push/commit) a esta sesión de Cowork — solo sirve para adjuntar archivos en el chat, sincronizar Projects, o sesiones de Claude Code.

**Resuelto (17-ago-2026):** token "Git Push" borrado desde github.com/settings/tokens (el otro token visible, "haskell_push", se dejó intacto — pertenece a otro proyecto, tiene uso activo reciente y permisos amplios de administrador, no es el que se expuso en este chat).

## 6. Otros conectores visibles pero no usados

En la lista de herramientas de esta sesión aparecen también Canva, Google Drive y Calendar — existen porque en algún momento se autorizaron desde la configuración de Claude, pero no los toqué ni los usé en ningún momento de este trabajo. Todo lo demás que ves listado (Slack, Asana, Figma, PagerDuty, Linear, etc.) aparece marcado como "requiere autenticación" — no están conectados, no tengo ningún acceso ahí.

## Nota sobre persistencia

No guardo contraseñas ni claves en mi propia memoria entre sesiones — cuando esta conversación termine, cualquier cosa que solo haya estado en el chat desaparece con ella. Lo único que persiste es lo que queda escrito en tus archivos reales, y ahí lo único sensible es la API key de Odoo, en el archivo mencionado en la sección 1, protegido por `.gitignore`.
